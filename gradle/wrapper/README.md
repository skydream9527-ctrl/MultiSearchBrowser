# Gradle Wrapper

This project uses the Gradle Wrapper. If you have Gradle installed, run:

```bash
gradle wrapper
```

This will generate the gradle-wrapper.jar file needed to build the project.

Alternatively, download it from:
https://services.gradle.org/distributions/gradle-8.2-bin.zip

And extract the wrapper files from the lib/plugins directory.